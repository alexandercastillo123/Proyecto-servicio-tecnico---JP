# Configuración de BD usada SOLO por la suite de tests.
# Apunta a una BD separada (servicio_tecnico_test) para no tocar datos de desarrollo.
NODE_ENV=test

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_PORT=3306
DB_NAME=servicio_tecnico_test

JWT_SECRET=test-secret
JWT_EXPIRATION=1h